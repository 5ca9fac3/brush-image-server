/* Defining the interface of the UpdateImage object. */
export interface UpdateImage {
  _id?: string;
  fileName?: string;
  mimeType?: string;
  accessKey?: string;
  location?: string;
  processType?: string;
}
